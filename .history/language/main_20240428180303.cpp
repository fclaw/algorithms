#include <iostream>

using namespace std;

int main() 
{
   
  int array[5] = {1, 2, 4, 5, 8};

    copy(array, array + sizeof(array) / sizeof(foo[0]),
       ostream_iterator<short>(cout, "\n"));

  return 0;

}